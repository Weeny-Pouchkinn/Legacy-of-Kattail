technology_sharing_group = {

	id = MAN_PRC_technology_sharing_group
	name = MAN_PRC_technology_sharing_group_name
	desc = MAN_PRC_technology_sharing_group_desc
	picture = GFX_technology_sharing_default

	research_sharing_per_country_bonus = 0.1

	available = { 
		OR = {
			AND = {
				original_tag = MAN
				has_government = communism
				NOT = {
					has_war_with = PRC
				}
			}
			AND = {
				original_tag = PRC
				has_government = communism
				NOT = {
					has_war_with = MAN
				}
			}
		}
	}
}

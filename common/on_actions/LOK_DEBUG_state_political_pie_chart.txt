# Legacy of Kattail - state politics debug initializer DISABLED.
#
# State politics now comes from the explicit population-group rows stored in
# history/states/*.txt. LOK_pop_groups_on_actions.txt sums those history rows at
# startup and calls LOK_rebuild_state_political_pie.
#
# Keep this file at the same path/name so it replaces the old debug on_action,
# which randomized lok_state_parties_array and would overwrite real pop-derived
# politics.

on_actions = {
}

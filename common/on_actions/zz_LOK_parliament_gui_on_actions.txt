on_actions = {
    on_yearly = {
        effect = {
            if = { limit = { has_country_flag = LOK_parliament_gui_open } LOK_parliament_gui_refresh = yes }
        }
    }
    on_coup_succeeded = {
        effect = {
            if = { limit = { has_country_flag = LOK_parliament_gui_open } LOK_parliament_gui_refresh = yes }
        }
    }
    on_civil_war_end = {
        effect = {
            if = { limit = { has_country_flag = LOK_parliament_gui_open } LOK_parliament_gui_refresh = yes }
        }
    }
    on_puppet = {
        effect = {
            if = { limit = { has_country_flag = LOK_parliament_gui_open } LOK_parliament_gui_refresh = yes }
        }
    }
    on_release_as_puppet = {
        effect = {
            if = { limit = { has_country_flag = LOK_parliament_gui_open } LOK_parliament_gui_refresh = yes }
        }
    }
}

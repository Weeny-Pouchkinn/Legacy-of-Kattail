# Kusnurk Confederation - Industry Decisions

# ==========================================
# Factory Construction Decisions (Focus on Industrial Infrastructure)
# ==========================================
economy_decisions = {
	KUS_industrial_construction_1 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_focus_on_industrial_infrastructure
			NOT = { has_country_flag = KUS_industrial_construction_1_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 45

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_industrial_construction_1_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_industrial_construction_2 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_focus_on_industrial_infrastructure
			has_country_flag = KUS_industrial_construction_1_done
			NOT = { has_country_flag = KUS_industrial_construction_2_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 45

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_industrial_construction_2_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_industrial_construction_3 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_focus_on_industrial_infrastructure
			has_country_flag = KUS_industrial_construction_2_done
			NOT = { has_country_flag = KUS_industrial_construction_3_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 45

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_industrial_construction_3_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_industrial_construction_4 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_focus_on_industrial_infrastructure
			has_country_flag = KUS_industrial_construction_3_done
			NOT = { has_country_flag = KUS_industrial_construction_4_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 45

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_industrial_construction_4_done
		}

		ai_will_do = { base = 100 }
	}
}

# ==========================================
# Sustainable Construction Decisions (Sustainable Development)
# ==========================================
economy_decisions = {
	KUS_sustainable_construction_1 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_sustainable_development
			NOT = { has_country_flag = KUS_sustainable_construction_1_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 60

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_sustainable_construction_1_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_sustainable_construction_2 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_sustainable_development
			has_country_flag = KUS_sustainable_construction_1_done
			NOT = { has_country_flag = KUS_sustainable_construction_2_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 60

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_sustainable_construction_2_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_sustainable_construction_3 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_sustainable_development
			has_country_flag = KUS_sustainable_construction_2_done
			NOT = { has_country_flag = KUS_sustainable_construction_3_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 3
		}

		cost = 0

		modifier = {
			civilian_factory_use = 4
		}

		days_remove = 60

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_sustainable_construction_3_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_sustainable_construction_4 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_sustainable_development
			has_country_flag = KUS_sustainable_construction_3_done
			NOT = { has_country_flag = KUS_sustainable_construction_4_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 3
		}

		cost = 0

		modifier = {
			civilian_factory_use = 4
		}

		days_remove = 60

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_sustainable_construction_4_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_sustainable_construction_5 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_sustainable_development
			has_country_flag = KUS_sustainable_construction_4_done
			NOT = { has_country_flag = KUS_sustainable_construction_5_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 60

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_sustainable_construction_5_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_sustainable_construction_6 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_sustainable_development
			has_country_flag = KUS_sustainable_construction_5_done
			NOT = { has_country_flag = KUS_sustainable_construction_6_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 4
		}

		cost = 0

		modifier = {
			civilian_factory_use = 5
		}

		days_remove = 60

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_sustainable_construction_6_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_sustainable_construction_7 = {
		icon = GFX_decision_generic_factory

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_sustainable_development
			has_country_flag = KUS_sustainable_construction_6_done
			NOT = { has_country_flag = KUS_sustainable_construction_7_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 4
		}

		cost = 0

		modifier = {
			civilian_factory_use = 5
		}

		days_remove = 60

		remove_effect = {
			random_owned_controlled_state = {
				limit = { is_core_of = KUS }
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
			set_country_flag = KUS_sustainable_construction_7_done
		}

		ai_will_do = { base = 100 }
	}
}

# ==========================================
# Väo Extraction Decisions
# ==========================================
prospect_for_resources = {
	KUS_deep_hematite_mining = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_expand_vao_extraction
			NOT = { has_country_flag = KUS_deep_hematite_mining_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_tech = excavation1
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 60

		remove_effect = {
			225 = { # Krrama
				add_resource = {
					type = steel
					amount = 7
				}
			}
			set_country_flag = KUS_deep_hematite_mining_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_rebuild_excavation_facility = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_expand_vao_extraction
			NOT = { has_country_flag = KUS_rebuild_excavation_facility_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_tech = excavation2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 60

		remove_effect = {
			225 = { # Krrama
				add_resource = {
					type = steel
					amount = 10
				}
			}
			set_country_flag = KUS_rebuild_excavation_facility_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_develop_ilmenite_processing = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_expand_vao_extraction
			NOT = { has_country_flag = KUS_develop_ilmenite_processing_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 4
			has_tech = excavation2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 5
		}

		days_remove = 60

		remove_effect = {
			225 = { # Krrama
				add_resource = {
					type = steel
					amount = 12
				}
			}
			set_country_flag = KUS_develop_ilmenite_processing_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_modernize_extraction = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_expand_vao_extraction
			NOT = { has_country_flag = KUS_modernize_extraction_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 8
			has_tech = excavation4
		}

		cost = 0

		modifier = {
			civilian_factory_use = 9
		}

		days_remove = 60

		remove_effect = {
			225 = { # Krrama
				add_resource = {
					type = steel
					amount = 12
				}
			}
			set_country_flag = KUS_modernize_extraction_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_develop_fergusonite_mines = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_expand_vao_extraction
			NOT = { has_country_flag = KUS_develop_fergusonite_mines_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_tech = excavation2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 60

		remove_effect = {
			225 = { # Krrama
				add_resource = {
					type = tungsten
					amount = 10
				}
			}
			set_country_flag = KUS_develop_fergusonite_mines_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_deep_fergusonite_harvesting = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_expand_vao_extraction
			NOT = { has_country_flag = KUS_deep_fergusonite_harvesting_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 4
			has_tech = excavation3
		}

		cost = 0

		modifier = {
			civilian_factory_use = 5
		}

		days_remove = 60

		remove_effect = {
			225 = { # Krrama
				add_resource = {
					type = tungsten
					amount = 8
				}
			}
			set_country_flag = KUS_deep_fergusonite_harvesting_done
		}

		ai_will_do = { base = 100 }
	}
}

# ==========================================
# Divikatel Decisions
# ==========================================
prospect_for_resources = {
	KUS_rare_earth_refineries = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_bail_out_divikatel
			NOT = { has_country_flag = KUS_rare_earth_refineries_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 2
			has_tech = excavation1
		}

		cost = 0

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 60

		remove_effect = {
			231 = { # Kikol
				add_resource = {
					type = tungsten
					amount = 4
				}
			}
			set_country_flag = KUS_rare_earth_refineries_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_excavation_site_expansion = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_bail_out_divikatel
			NOT = { has_country_flag = KUS_excavation_site_expansion_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 3
			has_tech = excavation2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 4
		}

		days_remove = 60

		remove_effect = {
			231 = { # Kikol
				add_resource = {
					type = tungsten
					amount = 6
				}
			}
			set_country_flag = KUS_excavation_site_expansion_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_precision_rare_earth_refineries = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_bail_out_divikatel
			NOT = { has_country_flag = KUS_precision_rare_earth_refineries_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 8
			has_tech = excavation3
		}

		cost = 0

		modifier = {
			civilian_factory_use = 9
		}

		days_remove = 60

		remove_effect = {
			231 = { # Kikol
				add_resource = {
					type = tungsten
					amount = 8
				}
			}
			set_country_flag = KUS_precision_rare_earth_refineries_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_create_bauxite_refineries = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_bail_out_divikatel
			NOT = { has_country_flag = KUS_create_bauxite_refineries_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 3
			has_tech = excavation2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 4
		}

		days_remove = 60

		remove_effect = {
			717 = { # Kohla
				add_resource = {
					type = aluminium
					amount = 8
				}
			}
			set_country_flag = KUS_create_bauxite_refineries_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_expand_bauxite_mining = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_bail_out_divikatel
			NOT = { has_country_flag = KUS_expand_bauxite_mining_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 5
			has_tech = excavation3
		}

		cost = 0

		modifier = {
			civilian_factory_use = 6
		}

		days_remove = 60

		remove_effect = {
			225 = { # Krrama
				add_resource = {
					type = aluminium
					amount = 6
				}
			}
			set_country_flag = KUS_expand_bauxite_mining_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_expand_jarvalin_aluminium_plant = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_bail_out_divikatel
			NOT = { has_country_flag = KUS_expand_jarvalin_aluminium_plant_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 7
			has_tech = excavation3
		}

		cost = 0

		modifier = {
			civilian_factory_use = 8
		}

		days_remove = 60

		remove_effect = {
			218 = { # Jarvalin
				add_resource = {
					type = aluminium
					amount = 12
				}
			}
			set_country_flag = KUS_expand_jarvalin_aluminium_plant_done
		}

		ai_will_do = { base = 100 }
	}
}

# ==========================================
# Monazite Decisions
# ==========================================
prospect_for_resources = {
	KUS_industrial_sand_sifting = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_monazite_exploitation
			NOT = { has_country_flag = KUS_industrial_sand_sifting_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 3
			has_tech = excavation2
		}

		cost = 0

		modifier = {
			civilian_factory_use = 4
		}

		days_remove = 60

		remove_effect = {
			717 = { # Kohla
				add_resource = {
					type = fissiles
					amount = 4
				}
			}
			set_country_flag = KUS_industrial_sand_sifting_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_develop_monazite_refining = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_monazite_exploitation
			NOT = { has_country_flag = KUS_develop_monazite_refining_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 5
			has_tech = excavation3
		}

		cost = 0

		modifier = {
			civilian_factory_use = 6
		}

		days_remove = 60

		remove_effect = {
			717 = { # Kohla
				add_resource = {
					type = fissiles
					amount = 12
				}
			}
			set_country_flag = KUS_develop_monazite_refining_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_monazite_fracking = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_monazite_exploitation
			NOT = { has_country_flag = KUS_monazite_fracking_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 7
			has_tech = excavation4
		}

		cost = 0

		modifier = {
			civilian_factory_use = 8
		}

		days_remove = 60

		remove_effect = {
			717 = { # Kohla
				add_resource = {
					type = fissiles
					amount = 5
				}
				add_resource = {
					type = chromium
					amount = 10
				}
			}
			set_country_flag = KUS_monazite_fracking_done
		}

		ai_will_do = { base = 100 }
	}

	KUS_monazite_processors = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_monazite_exploitation
			NOT = { has_country_flag = KUS_monazite_processors_done }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 9
			has_tech = excavation5
		}

		cost = 0

		modifier = {
			civilian_factory_use = 10
		}

		days_remove = 60

		remove_effect = {
			717 = { # Kohla
				add_resource = {
					type = fissiles
					amount = 4
				}
				add_resource = {
					type = chromium
					amount = 6
				}
			}
			set_country_flag = KUS_monazite_processors_done
		}

		ai_will_do = { base = 100 }
	}
}

# ==========================================
# Crush Rebel Groups Decisions (Technocratic Path)
# ==========================================
political_actions = {
	KUS_crush_rebels = {
		icon = GFX_decision_generic_ignite_civil_war

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_crush_rebel_groups
		}

		state_target = yes
		target_array = KUS_rebels_array

		fire_only_once = yes
		cost = 50
		days_remove = 35

		remove_effect = {
			FROM = {
				if = {
					limit = { is_demilitarized_zone = yes }
					set_demilitarized_zone = no
				}
				clr_state_flag = KUS_has_rebels
			}
		}
		ai_will_do = { base = 100 }
	}
}

# ==========================================
# Rewire for Total Automation Decisions (Basilisk)
# ==========================================
political_actions = {
	KUS_rewire_state = {
		icon = GFX_decision_generic_construction

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_construct_ai_infrastructure
			FROM = { NOT = { has_state_flag = KUS_total_automation } }
 		}

		state_target = yes

		fire_only_once = yes
		cost = 120
		days_remove = 60

		target_array = ROOT.core_states

		remove_effect = {
			FROM = {
				set_state_flag = KUS_total_automation
				set_temp_variable = { state_prod_change = 0.10 }
				increase_state_productivity = yes
				increase_base_state_productivity = yes
				custom_effect_tooltip = KUS_rewire_state_tt
				hidden_effect = {
					#Undo prod changes from pop
					subtract_from_variable = { lok_state_productivity_value = state_prod_change_from_pop }
					set_variable = { state_prod_change_from_pop = 0 }

					update_state_productivity_modifier = yes
				}
			}
		}

		ai_will_do = { base = 100 }
	}
}

# ==========================================
# Flusiobalkans Union Decisions
# ==========================================
KUS_flusiobalkans_union_cat = {

	# Puppet a faction member (United Flusiobalkans)
	KUS_propose_dominion_status = {
		icon = GFX_decision_generic_form_nation

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_united_flusiobalkans
			is_faction_leader = yes
			FROM = {
				is_in_faction_with = KUS
				NOT = { tag = KUS }
				is_subject = no
			}
		}

		target_trigger = {
			FROM = {
				is_in_faction_with = KUS
				NOT = { tag = KUS }
				is_subject = no
			}
		}

		targets = { FSR DOM NEK VIX MCF TEN KHA SLR IZW FST }
		target_root_trigger = {
			has_completed_focus = KUS_united_flusiobalkans
			is_faction_leader = yes
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = KUS_more_civs_than_target_tt
				num_of_civilian_factories > FROM.num_of_civilian_factories
			}
			custom_trigger_tooltip = {
				tooltip = KUS_more_mils_than_target_tt
				num_of_military_factories > FROM.num_of_military_factories
			}
			custom_trigger_tooltip = {
				tooltip = KUS_larger_army_than_target_tt
				strength_ratio = { tag = FROM ratio > 1.0 }
			}
		}

		fire_only_once = yes

		cost = 75

		remove_effect = {
			FROM = {
				country_event = { id = KUS_event.60 days = 3 }
			}
		}

		ai_will_do = {
			base = 10
		}
	}

	# Integrate a puppet (True Union)
	KUS_propose_full_integration = {
		icon = GFX_decision_generic_form_nation

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_true_union
			is_faction_leader = yes
			FROM = {
				is_subject_of = KUS
				NOT = { tag = KUS }
			}
		}

		target_trigger = {
			FROM = {
				is_subject_of = KUS
				NOT = { tag = KUS }
			}
		}

		targets = { FSR DOM NEK VIX MCF TEN KHA SLR IZW FST }
		target_root_trigger = {
			has_completed_focus = KUS_true_union
			is_faction_leader = yes
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = KUS_1_5x_civs_tt
				num_of_civilian_factories > FROM.num_of_civilian_factories
			}
			custom_trigger_tooltip = {
				tooltip = KUS_1_5x_mils_tt
				num_of_military_factories > FROM.num_of_military_factories
			}
			custom_trigger_tooltip = {
				tooltip = KUS_1_5x_army_tt
				strength_ratio = { tag = FROM ratio > 1.5 }
			}
		}

		fire_only_once = yes

		cost = 100

		complete_effect = {
			FROM = {
				country_event = { id = KUS_event.70 days = 3 }
			}
		}

		ai_will_do = {
			base = 10
		}
	}

	KUS_declare_war_fascist_flusiobalkan = {
		icon = GFX_decision_generic_prepare_civil_war

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_secure_flusiobalkans_dominance
			FROM = {
				is_flusiobalkan = yes
				has_government = fascism
				NOT = { tag = KUS }
				NOT = { has_war_with = KUS }
				exists = yes
			}
		}

		target_trigger = {
			FROM = {
				is_flusiobalkan = yes
				has_government = fascism
				NOT = { tag = KUS }
				NOT = { has_war_with = KUS }
				exists = yes
			}
		}

		targets = { FSR DOM NEK VIX MCF TEN KHA SLR IZW FST }
		target_root_trigger = {
			has_completed_focus = KUS_secure_flusiobalkans_dominance
		}

		available = {
			has_war = no
		}

		fire_only_once = yes

		cost = 50

		days_remove = 14

		complete_effect = {
			FROM = {
				country_event = { id = LOK_generic.1 days = 3 }
			}
		}

		remove_effect = {
			declare_war_on = {
				target = FROM
				type = topple_government
			}
		}

		ai_will_do = {
			base = 5
		}
	}


	KUS_pressure_join_faction = {
		icon = GFX_decision_generic_form_nation

		allowed = { tag = KUS }

		visible = {
			has_completed_focus = KUS_pressure_flusiobalkan_influence
			is_faction_leader = yes
			FROM = {
				is_flusiobalkan = yes
				NOT = { tag = KUS }
				NOT = { has_government = fascism }
				NOT = { is_in_faction_with = KUS }
				NOT = { is_subject_of = KUS }
				NOT = { is_faction_leader = yes }
				exists = yes
			}
		}

		target_trigger = {
			FROM = {
				is_flusiobalkan = yes
				NOT = { tag = KUS }
				NOT = { has_government = fascism }
				NOT = { is_in_faction_with = KUS }
				NOT = { is_subject_of = KUS }
				NOT = { is_faction_leader = yes }
				exists = yes
			}
		}

		targets = { FSR DOM NEK VIX MCF TEN KHA SLR IZW FST }
		target_root_trigger = {
			has_completed_focus = KUS_pressure_flusiobalkan_influence
			is_faction_leader = yes
		}

		days_remove = 180

		available = {
			custom_trigger_tooltip = {
				tooltip = KUS_pressure_4x_industry_tt
				# KUS faction needs 4x the target's factories (8x if target is in a faction)
				hidden_trigger = {
					set_temp_variable = { KUS_temp_target_fac = FROM.num_of_civilian_factories }
					add_to_temp_variable = { KUS_temp_target_fac = FROM.num_of_military_factories }
					if = {
						limit = { FROM = { is_in_faction = yes } }
						multiply_temp_variable = { KUS_temp_target_fac = 8 }
					}
					else = {
						multiply_temp_variable = { KUS_temp_target_fac = 4 }
					}
					check_variable = { KUS_faction_total_factories > KUS_temp_target_fac }
				}
			}
		}

		fire_only_once = yes

		cost = 75

		remove_effect = {
			# Calculate thresholds for AI event response
			hidden_effect = {
				# Set flags for the response logic
				FROM = {
					set_variable = { KUS_pressure_target_factories = num_of_civilian_factories }
					add_to_variable = { KUS_pressure_target_factories = num_of_military_factories }
				}
				# Check if double threshold met (8x base, 16x if in faction)
				set_variable = { KUS_pressure_double_threshold = FROM.KUS_pressure_target_factories }
				if = {
					limit = { FROM = { is_in_faction = yes } }
					multiply_variable = { KUS_pressure_double_threshold = 16 }
				}
				else = {
					multiply_variable = { KUS_pressure_double_threshold = 8 }
				}
				if = {
					limit = { check_variable = { KUS_faction_total_factories > KUS_pressure_double_threshold } }
					FROM = { set_country_flag = KUS_pressure_double_industry }
				}
				# Check if KUS has 2x divisions (4x if in faction)
				if = {
					limit = {
						FROM = { is_in_faction = yes }
						strength_ratio = { tag = FROM ratio > 4.0 }
					}
					FROM = { set_country_flag = KUS_pressure_army_bonus }
				}
				else_if = {
					limit = {
						FROM = { is_in_faction = no }
						strength_ratio = { tag = FROM ratio > 2.0 }
					}
					FROM = { set_country_flag = KUS_pressure_army_bonus }
				}
			}
			FROM = {
				country_event = { id = KUS_event.80 days = 3 }
			}
		}

		ai_will_do = {
			base = 10
		}
	}
}

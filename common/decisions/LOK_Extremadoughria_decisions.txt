lok_journal_entries_cat = {

	excise_the_katzen_tumor = {
		icon = GFX_decision_generic_tank

		allowed = {
			OR = {
				tag = KOA
				tag = PGN
			}
		}

		visible = {
			has_country_flag = defeated_other_nation
			NOT = { has_country_flag = defeated_other_nation_cooldown }
			OR = {
				country_exists = FKS
				country_exists = SMI
			}
		}

		available = {
			is_subject = no
			has_war = no
		}

		fire_only_once = yes

		complete_effect = {
			if = {
				limit = { country_exists = FKS }
				FKS = { country_event = { id = LOK_generic.1 } }
			}
			if = {
				limit = { country_exists = SMI }
				SMI = { country_event = { id = LOK_generic.1 } }
			}
		}

		remove_effect = {
			if = {
				limit = { country_exists = FKS }
				FKS = {
					every_core_state = {
						custom_effect_tooltip = state_core_threshold_set_40
						set_state_flag = compliance_40_@ROOT
						add_to_array = { array = ROOT.potential_cores_array }
						add_claim_by = ROOT
					}
				}
				declare_war_on = { target = FKS type = annex_everything }
			}
			if = {
				limit = { country_exists = SMI }
				SMI = {
					every_core_state = {
						custom_effect_tooltip = state_core_threshold_set_40
						set_state_flag = compliance_40_@ROOT
						add_to_array = { array = ROOT.potential_cores_array }
						add_claim_by = ROOT
					}
				}
				declare_war_on = { target = SMI type = annex_everything }
			}
		}

		ai_will_do = {
			base = 1000
		}
	}

	crush_the_zuspri = {
		icon = GFX_decision_generic_tank

		allowed = {
			OR = {
				tag = KOA
				tag = PGN
			}
		}

		visible = {
			has_country_flag = defeated_other_nation
			NOT = { has_country_flag = defeated_other_nation_cooldown }
			country_exists = UST
		}

		available = {
			is_subject = no
			has_war = no
		}

		fire_only_once = yes

		complete_effect = {
			UST = { country_event = { id = LOK_generic.1 } }
		}

		remove_effect = {
			UST = {
				every_core_state = {
					custom_effect_tooltip = state_core_threshold_set_40
					set_state_flag = compliance_40_@ROOT
					add_to_array = { array = ROOT.potential_cores_array }
					add_claim_by = ROOT
				}
			}
			declare_war_on = { target = UST type = annex_everything }
		}

		ai_will_do = {
			base = 1000
		}
	}

	acquire_piers_of_our_own = {
		icon = GFX_decision_generic_tank

		allowed = {
			OR = {
				tag = KOA
				tag = PGN
			}
		}

		visible = {
			has_country_flag = defeated_other_nation
			NOT = { has_country_flag = defeated_other_nation_cooldown }
			country_exists = PAN
		}

		available = {
			is_subject = no
			has_war = no
		}

		fire_only_once = yes

		complete_effect = {
			PAN = { country_event = { id = LOK_generic.1 } }
		}

		remove_effect = {
			PAN = {
				every_core_state = {
					custom_effect_tooltip = state_core_threshold_set_40
					set_state_flag = compliance_40_@ROOT
					add_to_array = { array = ROOT.potential_cores_array }
					add_claim_by = ROOT
				}
			}
			declare_war_on = { target = PAN type = annex_everything }
		}

		ai_will_do = {
			base = 1000
		}
	}

	topple_the_galletian_heartlands = {
		icon = GFX_decision_generic_tank

		allowed = {
			OR = {
				tag = KOA
				tag = PGN
			}
		}

		visible = {
			has_country_flag = defeated_other_nation
			NOT = { has_country_flag = defeated_other_nation_cooldown }
			country_exists = GAL
		}

		available = {
			is_subject = no
			has_war = no
			controls_state = 1192
			controls_state = 1187
			controls_state = 322
		}

		fire_only_once = yes

		complete_effect = {
			GAL = { country_event = { id = LOK_generic.1 } }
		}

		remove_effect = {
			GAL = {
				every_core_state = {
					custom_effect_tooltip = state_core_threshold_set_40
					set_state_flag = compliance_40_@ROOT
					add_to_array = { array = ROOT.potential_cores_array }
					add_claim_by = ROOT
				}
			}
			declare_war_on = { target = GAL type = annex_everything }
			news_event = { id = LOK_PGN_KOA.3 hours = 1 }
		}

		ai_will_do = {
			base = 1000
		}
	}

	form_unified_flusionea = {
		icon = generic_form_nation

		allowed = {
			OR = {
				tag = KOA
				tag = PGN
			}
		}

		visible = {
			NOT = { has_country_flag = formed_flusionea }
		}

		available = {
			NOT = { country_exists = GAL }
			is_subject = no
			has_war = no
			controls_state = 891
			controls_state = 1192
		}

		fire_only_once = yes

		complete_effect = {
			set_country_flag = formed_flusionea
			every_owned_state = {
				add_compliance = 20
			}
			if = {
				limit = { tag = KOA }
				set_cosmetic_tag = KOA_FLUSIONEAN_HYPERCOMMUNE
			}
			if = {
				limit = { tag = PGN }
				set_cosmetic_tag = PGN_MYSTIC_REALM_OF_FLUSIONEA
			}
		}

		ai_will_do = {
			base = 1000
		}
	}
}

herzlands_unification_cat = {
	AUR_expand_into_neighbor_north = {
		icon = GFX_decision_generic_confiscation

		allowed = {
			is_herzlands_warlord = yes
		}

		visible = {
			has_country_flag = AUR_northern_pawn
			is_in_faction_with = AUR
			FROM = { is_neighbor_of = ROOT }
			NOT = { has_country_flag = AUR_expand_into_neighbor_ongoing }
			NOT = { is_in_faction_with = FROM }
			NOT = { has_war_with = FROM }
			ROOT = { NOT = { is_puppet_of = FROM } }
			FROM = { NOT = { is_puppet_of = ROOT } }
		}

		fire_only_once = yes

		target_array = AUR.north_herzlands_nations_array

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				has_war = yes
			}
		}

		days_remove = 60

		complete_effect = {
			AUR = {
				country_event = { id = AUR_event.39 }
			}
			set_country_flag = AUR_expand_into_neighbor_ongoing
			hidden_effect = {
				FROM = {
					save_global_event_target_as = expand_into_neighbor_target
				}
			}
		}

		remove_effect = {
			clr_country_flag = AUR_expand_into_neighbor_ongoing
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
			FROM = {
				save_global_event_target_as = expand_into_neighbor_target
				every_core_state = {
					reduce_compliance_coring_threshold_by_10 = yes
					reduce_compliance_coring_threshold_by_10 = yes
				}
			}
		}
	}
	AUR_expand_into_neighbor_east = {
		icon = GFX_decision_generic_confiscation

		allowed = {
			is_herzlands_warlord = yes
		}

		visible = {
			has_country_flag = AUR_eastern_pawn
			is_in_faction_with = AUR
			NOT = { has_country_flag = AUR_expand_into_neighbor_ongoing }
			NOT = { is_in_faction_with = FROM }
			NOT = { has_war_with = FROM }
			ROOT = { NOT = { is_puppet_of = FROM } }
			FROM = { NOT = { is_puppet_of = ROOT } }
			FROM = { is_neighbor_of = ROOT }
		}

		fire_only_once = yes

		target_array = AUR.east_herzlands_nations_array

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				has_war = yes
			}
		}

		days_remove = 60

		complete_effect = {
			AUR = {
				country_event = { id = AUR_event.39 }
			}
			set_country_flag = AUR_expand_into_neighbor_ongoing
			hidden_effect = {
				FROM = {
					save_global_event_target_as = expand_into_neighbor_target
				}
			}
		}

		remove_effect = {
			clr_country_flag = AUR_expand_into_neighbor_ongoing
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
			FROM = {
				save_global_event_target_as = expand_into_neighbor_target
				every_core_state = {
					reduce_compliance_coring_threshold_by_10 = yes
					reduce_compliance_coring_threshold_by_10 = yes
				}
			}
		}
	}
	AUR_expand_into_neighbor_center = {
		icon = GFX_decision_generic_confiscation

		allowed = {
			is_herzlands_warlord = yes
		}

		visible = {
			has_country_flag = AUR_central_pawn
			is_in_faction_with = AUR
			NOT = { has_country_flag = AUR_expand_into_neighbor_ongoing }
			NOT = { is_in_faction_with = FROM }
			NOT = { has_war_with = FROM }
			ROOT = { NOT = { is_puppet_of = FROM } }
			FROM = { NOT = { is_puppet_of = ROOT } }
			FROM = { is_neighbor_of = ROOT }
		}

		fire_only_once = yes

		target_array = AUR.center_herzlands_nations_array

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				has_war = yes
			}
		}

		days_remove = 60

		complete_effect = {
			AUR = {
				country_event = { id = AUR_event.39 }
			}
			set_country_flag = AUR_expand_into_neighbor_ongoing
			hidden_effect = {
				FROM = {
					save_global_event_target_as = expand_into_neighbor_target
				}
			}
		}

		remove_effect = {
			clr_country_flag = AUR_expand_into_neighbor_ongoing
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
			FROM = {
				every_core_state = {
					reduce_compliance_coring_threshold_by_10 = yes
					reduce_compliance_coring_threshold_by_10 = yes
				}
			}
		}
	}
	AUR_katown_conquest = {
		activation  = {
			has_completed_focus = AUR_i_need_it_all
		}
		available = {
			owns_state = 36
			owns_state = 34
			owns_state = 44
			owns_state = 45
			owns_state = 509
		}
		icon = GFX_decision_pyramid
		is_good = no
		fire_only_once = yes
		days_mission_timeout = 1095
		timeout_effect = {
			custom_effect_tooltip = AUR_katown_conquest_tt
			country_event = { id = AUR_event.41 }
		}
	}
}

AUR_commissars_cat = {

	AUR_commissar_show_of_force = {
		icon = GFX_decision_generic_police_action
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_katzkommissars
			FROM = { NOT = { is_core_of = AUR } }
		}

		available = {
			FROM = { NOT = { has_state_flag = AUR_active_commissar } }
			check_variable = { AUR_available_commissars > 0 }
		}

		state_target = any_owned_state
		on_map_mode = map_only

		cost = 25

		complete_effect = {
			custom_effect_tooltip = AUR_dispatch_1_katzkommissar
			FROM = { set_state_flag = AUR_active_commissar }
			subtract_from_variable = { AUR_available_commissars = 1 }
			add_to_variable = { AUR_dispatched_commissars = 1 }
		}

		remove_effect = {
			subtract_from_variable = { AUR_dispatched_commissars = 1 }
			add_to_variable = { AUR_available_commissars = 1 }
			FROM = {
				clr_state_flag = AUR_active_commissar
				add_resistance = -20
				add_resistance_target = -20
				add_manpower = -5000
				every_neighbor_state = {
					limit = { is_controlled_by = AUR NOT = { is_core_of = AUR } }
					add_resistance = -10
					add_resistance_target = -10
					add_manpower = -10000
				}
			}
		}

		cancel_trigger = {
			FROM = { is_core_of = AUR }
		}

		cancel_effect = {
			subtract_from_variable = { AUR_dispatched_commissars = 1 }
			add_to_variable = { AUR_available_commissars = 1 }
			FROM = {
				clr_state_flag = AUR_active_commissar
			}
		}

		days_remove = 30

		ai_will_do = {
			base = 1000
		}
	}

	AUR_commissar_reward_collaborators = {
		icon = GFX_decision_CHL_mapuche_liberate_isthmo_amerindia
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_katzkommissars
			FROM = { NOT = { is_core_of = AUR } }
		}

		available = {
			FROM = {
				NOT = { has_state_flag = AUR_active_commissar }
				is_in_array = { AUR.potential_cores_array = THIS }
				hidden_trigger = {
					NOT = {
						has_state_flag = compliance_0_@ROOT
						has_state_flag = compliance_10_@ROOT
						has_state_flag = compliance_20_@ROOT
					}	
				}
			}
			check_variable = { AUR_available_commissars > 0 }
		}

		state_target = any_owned_state
		on_map_mode = map_only

		cost = 10

		complete_effect = {
			FROM = { set_state_flag = AUR_active_commissar }
			custom_effect_tooltip = AUR_dispatch_1_katzkommissar
			subtract_from_variable = { AUR_available_commissars = 1 }
			add_to_variable = { AUR_dispatched_commissars = 1 }
		}

		remove_effect = {
			subtract_from_variable = { AUR_dispatched_commissars = 1 }
			add_to_variable = { AUR_available_commissars = 1 }
			custom_effect_tooltip = AUR_commissar_reward_collaborators_remove_tt
			hidden_effect = {
				add_to_variable = { AUR_the_kaiser_must_return_research_speed_factor = -0.001 }
				add_to_variable = { AUR_the_kaiser_must_return_industrial_capacity_factory = -0.001 }	
			}
			FROM = {
				clr_state_flag = AUR_active_commissar
				custom_effect_tooltip = state_core_threshold_lower_20
				hidden_effect = {
					reduce_compliance_coring_threshold_by_10 = yes
					reduce_compliance_coring_threshold_by_10 = yes
				}
				every_neighbor_state = {
					limit = { is_controlled_by = AUR NOT = { is_core_of = AUR } }
					custom_effect_tooltip = state_core_threshold_lower_10
					hidden_effect = {
						reduce_compliance_coring_threshold_by_10 = yes
					}
				}
			}
		}

		cancel_trigger = {
			FROM = { is_core_of = AUR }
		}

		cancel_effect = {
			subtract_from_variable = { AUR_dispatched_commissars = 1 }
			add_to_variable = { AUR_available_commissars = 1 }
			FROM = {
				clr_state_flag = AUR_active_commissar
			}
		}


		days_remove = 30

		ai_will_do = {
			base = 1000
		}
	}

	AUR_clease_unterkatzens = {
		icon = GFX_decision_generic_scorched_earth
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_remove_monke
			FROM = {
				NOT = { check_variable = { minority = 1000 } }
			}
		}

		available = {
			FROM = { NOT = { has_state_flag = AUR_active_commissar } }
			check_variable = { AUR_available_commissars > 0 }
		}

		state_target = any_owned_state
		on_map_mode = map_only

		cost = 25

		complete_effect = {
			custom_effect_tooltip = AUR_dispatch_1_katzkommissar
			FROM = { set_state_flag = AUR_active_commissar }
			subtract_from_variable = { AUR_available_commissars = 1 }
			add_to_variable = { AUR_dispatched_commissars = 1 }
		}

		remove_effect = {
			subtract_from_variable = { AUR_dispatched_commissars = 1 }
			add_to_variable = { AUR_available_commissars = 1 }
			FROM = {
				clr_state_flag = AUR_active_commissar
				add_resistance = -10
				add_resistance_target = -10
				custom_effect_tooltip = AUR_clease_unterkatzens_tt
				set_variable = { minority = 1000 }
				add_manpower = -500000
			}
			add_stability = -0.05
			force_update_map_mode = {
				mapmode = LOK_species_map_mode
			}
			custom_effect_tooltip = AUR_clease_unterkatzens_remove_tt
			hidden_effect = {
				add_to_variable = { AUR_the_kaiser_must_return_research_speed_factor = -0.01 }
				add_to_variable = { AUR_the_kaiser_must_return_industrial_capacity_factory = -0.01 }	
			}
		}

		cancel_effect = {
			subtract_from_variable = { AUR_dispatched_commissars = 1 }
			add_to_variable = { AUR_available_commissars = 1 }
			FROM = {
				clr_state_flag = AUR_active_commissar
			}
		}


		days_remove = 30

		ai_will_do = {
			base = 1000
		}
	}
}

AUR_industrialization_cat = {
	AUR_industry_timer = {
		available = {
			hidden_trigger = { always = no }
		}
		activation = {
			has_completed_focus = AUR_dreijahrige
			NOT = { has_country_flag = AUR_industry_timer_done }
		}
		icon = GFX_decision_generic_factory
		is_good = no
		days_mission_timeout = 365
		timeout_effect = {
			custom_effect_tooltip = AUR_industry_timer_tt
		}
	}
	#1st term
	AUR_aggressive_industrialization = {
		icon = GFX_decision_generic_construction
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_dreijahrige
			NOT = { has_completed_focus = AUR_pushing_pushing }
			FROM = { is_core_of = AUR check_variable = { building_level@infrastructure < 5 } }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 4
		}

		state_target = any_owned_state
		on_map_mode = map_only

		cost = 20

		modifier = {
			civilian_factory_use = 5
		}

		remove_effect = {
			FROM = {
				add_building_construction = { type = infrastructure level = 1 }
				add_extra_state_shared_building_slots = 2
			}
		}

		days_remove = 30

		ai_will_do = {
			base = 1000
		}
	}

	AUR_nuclear_salvation = {
		icon = GFX_decision_generic_electricity
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_dreijahrige
			NOT = { has_completed_focus = AUR_pushing_pushing }
			FROM = {
				is_core_of = AUR
				check_variable = { building_level@nuclear_reactor < 1 } 
			}
		}

		available = {
			num_of_civilian_factories_available_for_projects > 6
		}

		state_target = any_owned_state
		on_map_mode = map_only

		cost = 20

		modifier = {
			civilian_factory_use = 7
		}

		remove_effect = {
			FROM = {
				add_building_construction = { type = nuclear_reactor level = 1 }
				add_extra_state_shared_building_slots = 1
			}
		}

		days_remove = 90

		ai_will_do = {
			base = 1000
		}
	}

	AUR_mass_worker_conscription = {
		icon = GFX_decision_generic_merge_plant_materiel
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_dreijahrige
			NOT = { has_completed_focus = AUR_fulfilled }
		}

		cost = 50

		modifier = {
			consumer_goods_factor = -0.50
		}

		remove_effect = {
			add_stability = -0.05
			add_manpower = -1000
		}

		days_remove = 60

		ai_will_do = {
			base = 1000
		}
	}

	#2nd term
	AUR_expand_mil_factories = {
		icon = GFX_decision_generic_tank
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_pushing_pushing
			NOT = { has_completed_focus = AUR_nearly_there }
			FROM = { is_core_of = AUR NOT = { has_state_flag = aur_expanded_mils } }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 4
		}

		state_target = any_owned_state
		on_map_mode = map_only

		cost = 15

		modifier = {
			civilian_factory_use = 5
		}

		remove_effect = {
			FROM = {
				add_building_construction = { type = arms_factory level = 2 }
				add_extra_state_shared_building_slots = 2
				hidden_effect = { set_state_flag = aur_expanded_mils }
			}
		}

		days_remove = 30

		ai_will_do = {
			base = 1000
		}
	}

	AUR_civilian_factories = {
		icon = GFX_decision_generic_factory
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_pushing_pushing
			NOT = { has_completed_focus = AUR_nearly_there }
			FROM = { is_core_of = AUR NOT = { has_state_flag = aur_expanded_civs } }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 4
		}

		state_target = any_owned_state
		on_map_mode = map_only

		cost = 15

		modifier = {
			civilian_factory_use = 5
		}

		remove_effect = {
			FROM = {
				add_building_construction = { type = industrial_complex level = 2 }
				add_extra_state_shared_building_slots = 2
				hidden_effect = { set_state_flag = aur_expanded_civs }
			}
		}

		days_remove = 30

		ai_will_do = {
			base = 1000
		}
	}

	#3rd term
	AUR_katzen_lifestyle = {
		icon = GFX_decision_generic_army_support
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_nearly_there
			NOT = { has_completed_focus = AUR_fulfilled }
			FROM = { is_core_of = AUR }
			check_variable = { building_level@anti_air_building < 3 }
		}

		available = {
			has_equipment = { anti_air_equipment > 99 }
		}

		state_target = any_owned_state
		on_map_mode = map_only

		cost = 10

		modifier = {
			industrial_capacity_factory = -0.30
		}

		remove_effect = {
			add_war_support = 0.025
			add_popularity = {
				ideology = fascism
				popularity = 0.025
			}
			add_equipment_to_stockpile = { type = anti_air_equipment amount = -100 }
			FROM = {
				add_building_construction = { type = anti_air_building level = 2 }
			}
		}

		days_remove = 30

		ai_will_do = {
			base = 1000
		}
	}

	AUR_projekt_seer = {
		icon = GFX_decision_generic_electricity
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_nearly_there
			NOT = { has_completed_focus = AUR_fulfilled }
			FROM = { is_core_of = AUR }
		}

		available = {
			num_of_civilian_factories_available_for_projects > 4
		}

		state_target = any_owned_state
		on_map_mode = map_only

		cost = 10

		modifier = {
			civilian_factory_use = 5
		}

		remove_effect = {
			FROM = {
				add_building_construction = { type = radar_station level = 2 }
			}
		}

		days_remove = 30

		ai_will_do = {
			base = 1000
		}
	}

	AUR_plan_for_the_worst = {
		icon = GFX_decision_generic_fortification
		allowed = {
			tag = AUR
		}

		visible = {
			has_completed_focus = AUR_nearly_there
			NOT = { has_completed_focus = AUR_fulfilled }
			FROM = {
				is_core_of = AUR
				any_neighbor_state = { NOT = { is_owned_by = AUR } }
				NOT = { has_state_flag = aur_fortified_state }
			}
		}

		available = {
			num_of_civilian_factories_available_for_projects > 4
		}

		state_target = any_owned_state
		on_map_mode = map_only

		cost = 10

		modifier = {
			civilian_factory_use = 5
		}

		remove_effect = {
			hidden_effect = { add_to_variable = { aur_num_new_bunkers_built = 1 } }
			FROM = {
				set_building_level = {
					type = bunker
					level = 2
					instant_build = yes
					province = {
						all_provinces = yes
						limit_to_border = yes
					}
				}
				hidden_effect = { set_state_flag = aur_fortified_state }
			}
		}

		days_remove = 30

		ai_will_do = {
			base = 1000
		}
	}
}

AUR_chew_slowly_cat = {
	AUR_recruit_northern_nation = {
		icon = GFX_decision_generic_confiscation

		allowed = {
			tag = AUR
		}

		visible = {
			tag = AUR
			NOT = { has_country_flag = AUR_invading_the_north }
			has_completed_focus = AUR_pawns_in_the_north
			check_variable = { AUR_northern_pawn_count < 1 }
			NOT = {
				any_country = {
					has_country_flag = AUR_northern_pawn
					has_war_with = FROM
				}
			}
			FROM = {
				NOT = { has_government = democratic }
				NOT = { has_government = communism }
				NOT = { has_war_with = AUR }
				is_puppet = no
				NOT = { has_country_flag = AUR_offered_recruitment_to_nation }
			}
		}

		available = {
			NOT = { has_country_flag = AUR_recruit_northern_nation_ongoing }
		}

		fire_only_once = yes

		target_array = AUR.north_herzlands_nations_array

		ai_will_do = {
			factor = 100
		}

		cost = 15

		days_remove = 10

		complete_effect = {
			set_country_flag = AUR_recruit_northern_nation_ongoing
			FROM = {
				set_country_flag = AUR_offered_recruitment_to_nation
			}
		}

		remove_effect = {
			custom_effect_tooltip = AUR_recruit_northern_nation_tt
			FROM = { country_event = { id = AUR_event.30 } }
			clr_country_flag = AUR_recruit_northern_nation_ongoing
		}
	}

	AUR_recruit_eastern_nation = {
		icon = GFX_decision_generic_confiscation

		allowed = {
			tag = AUR
		}

		visible = {
			NOT = { has_country_flag = AUR_invading_the_east }
			tag = AUR
			has_completed_focus = AUR_pawns_in_the_east
			check_variable = { AUR_eastern_pawn_count < 1 }
			FROM = {
				NOT = { has_government = democratic }
				NOT = { has_government = communism }
				NOT = { has_war_with = AUR }
				is_puppet = no
				NOT = { has_country_flag = AUR_offered_recruitment_to_nation }
			}
		}

		available = {
			NOT = { has_country_flag = AUR_recruit_eastern_nation_ongoing }
		}

		fire_only_once = yes

		target_array = AUR.east_herzlands_nations_array

		ai_will_do = {
			factor = 100
		}

		cost = 15

		days_remove = 10

		complete_effect = {
			set_country_flag = AUR_recruit_eastern_nation_ongoing
			FROM = {
				set_country_flag = AUR_offered_recruitment_to_nation
			}
		}

		remove_effect = {
			custom_effect_tooltip = AUR_recruit_eastern_nation_tt
			FROM = { country_event = { id = AUR_event.30 } }
			clr_country_flag = AUR_recruit_eastern_nation_ongoing
		}
	}

	AUR_recruit_central_nation = {
		icon = GFX_decision_generic_confiscation

		allowed = {
			tag = AUR
		}

		visible = {
			tag = AUR
			NOT = { has_country_flag = AUR_invading_the_center }
			has_completed_focus = AUR_pawns_in_the_center
			check_variable = { AUR_central_pawn_count < 1 }
			NOT = {
				any_country = {
					has_country_flag = AUR_central_pawn
					has_war_with = FROM
				}
			}
			FROM = {
				NOT = { has_government = democratic }
				NOT = { has_government = communism }
				NOT = { has_war_with = AUR }
				is_puppet = no
				NOT = { has_country_flag = AUR_offered_recruitment_to_nation }
			}
		}

		available = {
			NOT = { has_country_flag = AUR_recruit_central_nation_ongoing }
		}

		fire_only_once = yes

		target_array = AUR.center_herzlands_nations_array

		ai_will_do = {
			factor = 100
		}

		cost = 15

		days_remove = 10

		complete_effect = {
			set_country_flag = AUR_recruit_central_nation_ongoing
			FROM = {
				set_country_flag = AUR_offered_recruitment_to_nation
			}
		}

		remove_effect = {
			custom_effect_tooltip = AUR_recruit_central_nation_tt
			FROM = { country_event = { id = AUR_event.30 } }
			clr_country_flag = AUR_recruit_central_nation_ongoing
		}
	}

	AUR_invade_the_center = {
		icon = GFX_decision_generic_confiscation

		allowed = {
			tag = AUR
		}

		visible = {
			tag = AUR
			has_completed_focus = AUR_pawns_in_the_center
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = AUR_invade_the_center_trigger_tt
				hidden_trigger = {
					check_variable = { AUR_central_pawn_count = 0 }
					NOT = { has_country_flag = AUR_recruit_central_nation_ongoing }	
				}
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		cost = 15

		days_remove = 10

		complete_effect = {
			hidden_effect = {
				set_country_flag = AUR_invading_the_center
			}
		}

		remove_effect = {
			every_country = {
				limit = {
					is_in_array = {
						array = AUR.center_herzlands_nations_array
						value = THIS
					}
				}
				AUR = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
			set_temp_variable = { herald_sanity_change = -10 }
			AUR_change_herald_sanity = yes
		}
	}

	AUR_invade_the_north = {
		icon = GFX_decision_generic_confiscation

		allowed = {
			tag = AUR
		}

		visible = {
			tag = AUR
			has_completed_focus = AUR_pawns_in_the_north
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = AUR_invade_the_north_trigger_tt
				hidden_trigger = {
					check_variable = { AUR_northern_pawn_count = 0 }
					NOT = { has_country_flag = AUR_recruit_northern_nation_ongoing }	
				}		
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		cost = 15

		days_remove = 10

		complete_effect = {
			hidden_effect = {
				set_country_flag = AUR_invading_the_north
			}
		}

		remove_effect = {
			every_country = {
				limit = {
					is_in_array = {
						array = AUR.north_herzlands_nations_array
						value = THIS
					}
				}
				AUR = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
			set_temp_variable = { herald_sanity_change = -10 }
			AUR_change_herald_sanity = yes
		}
	}

	AUR_invade_the_east = {
		icon = GFX_decision_generic_confiscation

		allowed = {
			tag = AUR
		}

		visible = {
			tag = AUR
			has_completed_focus = AUR_pawns_in_the_east
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = AUR_invade_the_east_trigger_tt
				hidden_trigger = {
					check_variable = { AUR_eastern_pawn_count = 0 }
					NOT = { has_country_flag = AUR_recruit_eastern_nation_ongoing }	
				}
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		cost = 15

		days_remove = 10

		complete_effect = {
			hidden_effect = {
				set_country_flag = AUR_invading_the_east
			}
		}

		remove_effect = {
			every_country = {
				limit = {
					is_in_array = {
						array = AUR.east_herzlands_nations_array
						value = THIS
					}
				}
				AUR = {
					declare_war_on = {
						target = PREV
						type = annex_everything
					}
				}
			}
			set_temp_variable = { herald_sanity_change = -10 }
			AUR_change_herald_sanity = yes
		}
	}
}

lok_IPF_KSK_cat = {
	aur_shoot_the_ipf = {
		icon = GFX_decision_generic_police_action
		allowed = { original_tag = AUR }
	
		visible = {
			has_completed_focus = AUR_crushing_liberalism
			has_government = fascism
		}
	
		available = {
			check_variable = {
				ipf_strength > 0
			}
		}
	
		cost = 25
	
		days_remove = 30
		
		modifier = {
			political_power_factor = -0.50
			democratic_drift = -0.5
		}
	
		remove_effect = {
			custom_effect_tooltip = aur_shoot_the_ipf_tt
			hidden_effect = { subtract_from_variable = { ipf_strength = 1 } calculate_ipf_ksk_resistance = yes }
		}
	
		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				check_variable = { ipf_opinion > 0 }
			}
		}
	}

	aur_shoot_the_ksk = {
		icon = GFX_decision_oppression
		allowed = { original_tag = AUR }
	
		visible = {
			has_completed_focus = AUR_breaking_idealism
			has_government = fascism
		}
	
		available = {
			check_variable = {
				ksk_strength > 0
			}
		}
	
		cost = 25
	
		days_remove = 30
		
		modifier = {
			war_support_weekly = -0.01
			communism_drift = -0.5
		}
	
		remove_effect = {
			custom_effect_tooltip = aur_shoot_the_ksk_tt
			every_owned_state = {
				add_manpower = -10000
			}
			hidden_effect = { subtract_from_variable = { ksk_strength = 1 } calculate_ipf_ksk_resistance = yes }
		}
	
		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				check_variable = { ipf_opinion > 0 }
			}
		}
	}
}
